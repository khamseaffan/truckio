import { logger } from '@/shared/utils/logger';

// TODO Phase 4: Implement react-native-html-to-pdf

export async function generateInvoicePdf(_html: string): Promise<string | null> {
  logger.info('PDF generation — stub (Phase 4)');
  return null;
}
