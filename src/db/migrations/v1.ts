/**
 * V1 Migration — initial schema.
 * Since this is the first version, no migration steps needed.
 * Future schema changes will add migration steps here.
 */
export const migration = {
  toVersion: 1,
  steps: [],
};
